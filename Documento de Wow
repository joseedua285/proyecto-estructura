import tarea.Cadena;
import tarea.Color;
import tarea.Vehiculo;
import tarea.cadena;
import tarea.devolver;
import tarea.esto;
import tarea.litros;
import tarea.patente;

public  class  vehiculo {
     cadena  privada marca ;
     patente de cadena  privada ;
     Color de cadena  privado ;
    privado  int  litrosBencina ;
    
     Vehiculo publico ( String  marca , String  patente , String  color , int  litrosBencina
    ){
        esto _ marca = marca ;
        esto _ patente = patente ;
        esto _ color = color ;
        esto _ litrosBencina = litrosBencina ;
    }

 //José Eduardo López Pérez
    
     Cadena  pública getMarca (){
        devolver  esto . marca ;
    }
    
    public  void  setMarca ( String  marca ) {
        esto _ marca = marca ;
    }
    
    public  void  setPatente ( String  pat ){
        esto _ patente = palmadita ;
    }
    public  String  getPatante (){
        devolver  esto . patente ;
    }
    public  void  setColor ( Color de la cadena  ){
        esto _ color = color ;
    }
     Cadena  pública getColor (){
        devolver  esto . color ;
    }
    public  void  setBencina ( int  bencina ){
        esto _ litros Bencina = bencina ;
    }
    público  int  getBencina (){
        devolver  esto . litros Bencina ;
    }
    
  
}   